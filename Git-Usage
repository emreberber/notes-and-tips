### Git Usage on Terminal

ID:
git config --global user.name "Emre Berber"
git config --global user.email "brbr.emre@gmail.com"

git init
git add *
git status
git commit -m "first commit"
git remote add repo http..
git remote => repo
git pull repo master
git push -u repo master
